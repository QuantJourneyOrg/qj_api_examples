name: Public Repository Security

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "23 5 * * 1"

permissions:
  contents: read

jobs:
  public-security:
    uses: QuantJourneyOrg/.github/.github/workflows/public-python-security.yml@main
    with:
      notebook-command: python scripts/check_public_notebooks.py
